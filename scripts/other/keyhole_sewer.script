
{
	const KEY_NAME item_key_sewer
	const KEYHOLE_NAME "a|Sewer Maintenance Keyhole"
	const KEYHOLE_TITLE "Use the maintenance key"
	setvard RETURN_KEY 0
}

#include other/base_keyhole

{ key_used

	usetrigger ustart //MSR has old deraliasewers source, so this trigger is required to open the door.
}
