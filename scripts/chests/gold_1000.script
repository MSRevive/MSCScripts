//Mega gold award

{
	const GOLD_AMT 1000
}

{ game_spawn

	//only allowed to spawn on certain maps
	if ( $lcase(game.map.name) equals islesofdread1 ) local VALID_MAP 1
	if ( $lcase(game.map.name) equals bloodshrine ) local VALID_MAP 1

	if ( !VALID_MAP ) deleteme

	if VALID_MAP
}

#include chests/bag_o_gold_base
